import { CreateBookRequest } from './create-book-request';

describe('CreateBookRequest', () => {
  it('should create an instance', () => {
    expect(new CreateBookRequest()).toBeTruthy();
  });
});
